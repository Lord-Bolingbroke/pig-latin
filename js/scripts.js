var vowels = ['a', 'e', 'i', 'o', 'u']

var userInput =

function returnWay(userInput) {
  var returnArray = [];




    if (userInput === "A" || "E" || "I" || "O" || "U") {
      returnArray.push([i]+ "way");
    }
  }
    return returnArray;

}
